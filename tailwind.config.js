module.exports = {
  content: [
    './src/**/*.{html,js,jsx,ts,tsx}',
    './public/index.html', 
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

// 패키지.json 이 내용을 지우고 저장을 안된것 같음
// 지웠더라도 터미널을 창에서 인식할때 내부 파일이 지워졌는가 -> error 문구로 확인할 것 
// 
